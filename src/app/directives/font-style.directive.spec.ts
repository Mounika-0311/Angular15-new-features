import { FontStyleDirective } from './font-style.directive';

describe('SontStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new FontStyleDirective();
    expect(directive).toBeTruthy();
  });
});
